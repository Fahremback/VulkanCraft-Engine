#pragma once

// Compatibility include.  The canonical declaration lives beside the rest of
// the isolation API in IPluginIsolation.hpp; keeping this filename avoids
// breaking existing consumers while eliminating the parallel public contract.
#include "engine/plugins/IPluginIsolation.hpp"
